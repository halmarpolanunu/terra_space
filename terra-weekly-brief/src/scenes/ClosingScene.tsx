import {Easing, interpolate, useCurrentFrame} from 'remotion';
import {amber, FadeTitle, SceneShell} from './SceneShell';
export const ClosingScene: React.FC = () => { const frame = useCurrentFrame(); return <SceneShell index="06" label="Watchlist"><FadeTitle>Yang perlu dipantau berikutnya.</FadeTitle><div style={{position: 'absolute', top: 650, left: 80, right: 80, display: 'flex', flexDirection: 'column', gap: 32, opacity: interpolate(frame, [30, 66], [0, 1], {extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.16, 1, 0.3, 1)})}}>{['Apakah transit Hormuz kembali stabil?', 'Seberapa jauh serangan infrastruktur mengubah posisi perang Ukraina?', 'Apakah tekanan iklim mulai mengganggu ketahanan energi Eropa?'].map((item, index) => <div key={item} style={{display: 'flex', gap: 22, alignItems: 'flex-start', fontSize: 43, lineHeight: 1.18}}><span style={{color: amber, fontWeight: 800}}>0{index + 1}</span><span>{item}</span></div>)}</div><div style={{position: 'absolute', top: 1420, left: 80, right: 80, paddingTop: 36, borderTop: '1px solid #ffb23f66', fontSize: 29, lineHeight: 1.35, color: '#aaa49c', opacity: interpolate(frame, [85, 120], [0, 1], {extrapolateLeft: 'clamp', extrapolateRight: 'clamp'})}}>Basis: 22 artikel berstatus completed dan main issue grounded, diproses 10–14 Agustus 2026. Tidak ada event Phase 3 accepted dalam potongan data ini.</div></SceneShell>; };
